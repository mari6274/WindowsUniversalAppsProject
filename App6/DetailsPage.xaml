<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App6"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:App6.Models"
    x:Class="App6.DetailsPage"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Delete" Label="Delete" Click="Delete_OnClick" />
            <AppBarButton Name="AcceptButton" Icon="Accept" Label="Accept" Click="Accept_OnClick" Visibility="Collapsed"/>
            <AppBarButton Name="EditButton" Icon="Edit" Label="Edit" Click="Edit_OnClick"/>
            <AppBarButton Name="CancelButton" Icon="Cancel" Label="Edit" Click="Cancel_OnClick" Visibility="Collapsed"/>
        </CommandBar>
    </Page.BottomAppBar>
    <Page.DataContext>
        <Models:ToDoTask />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox Text="{Binding Title}" Margin="10" PlaceholderText="Title" />
        <TextBox Text="{Binding Value}" Grid.Row="1" Margin="10" PlaceholderText="Description" AcceptsReturn="True" />
    </Grid>

</Page>