<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ToDoApp.Models"
    x:Class="ToDoApp.AddTaskPage"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Add" Label="Add" Click="Add_OnClick" />
        </CommandBar>
    </Page.BottomAppBar>
    <Page.DataContext>
        <models:ToDoTask />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Add Task"
                   FontSize="32" />
        <TextBox Text="{Binding Title, Mode=TwoWay}" Grid.Row="1" Margin="10" PlaceholderText="Title" />
        <TextBox Text="{Binding Value, Mode=TwoWay}" Grid.Row="2" Margin="10" PlaceholderText="Description"
                 AcceptsReturn="True" />
    </Grid>
</Page>