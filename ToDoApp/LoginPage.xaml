<Page
    x:Class="ToDoApp.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToDoApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" Style="{StaticResource StackPanelStyle}">
            <TextBlock x:Uid="Login" Style="{StaticResource TitleTextBlockStyle}" Margin="10" />
            <TextBox Text="{Binding UserName, Mode=TwoWay}" Margin="10"/>
            <Button x:Uid="LoginAction" Click="LoginButton_OnClick" Margin="10"/>
        </StackPanel>
    </Grid>
</Page>